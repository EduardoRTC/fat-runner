import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  sprite: {
    position: 'absolute',
    bottom: 0,
    width: 60,
    height: 80,
    marginLeft: -30, // centraliza na coluna
  },
});
